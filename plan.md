# 拉取计划：LeetCode Hot 100 剩余题目

> 本文件是本次「批量拉取剩余题目」的**总计划**，目标是把 Hot 100 里还没做笔记的题目全部补齐。
> 任何 agent（或人）拿到本文件 + 下面索引的规则文件，就能快速接手继续做。
> 实时进度看 [`进度.md`](./进度.md)。

---

## 一、快速上手 5 步（任何 agent 接手先读这段）

1. **读规则**：[`AGENTS.md`](./AGENTS.md)（仓库总规范：标签→目录、STL 详注、不改已有笔记、不引工具链）+ [`.cursor/skills/leetcode-note/SKILL.md`](./.cursor/skills/leetcode-note/SKILL.md)（已固化的抓取流程 + 可直接复制的 curl 命令 + 踩坑表）。
2. **看进度**：打开 [`进度.md`](./进度.md)，找「下次起点」那一行——它就是全表第一个未完成的题。
3. **抓题**：按 SKILL.md「第 1 步」用 curl + GraphQL 抓题面（命令已验证，照抄即可）。
4. **抓官方题解**：按 SKILL.md「第 2 步」两步走（列文章→取正文），官方文章 slug 以 `by-leetcode-solution` 结尾。
5. **写笔记**：套现有样板文风（`双指针/283-移动零.md`、`哈希/1-两数之和.md`、`双指针/42-接雨水.md`），落盘到 [`进度.md`](./进度.md) 该行指定的目录，写完更新进度。

---

## 二、关键规则索引（不重复造轮子，指向源文件）

| 事项 | 权威出处 | 要点 |
|---|---|---|
| 标签 → 目录映射 + 多标签优先级 | `AGENTS.md` →「标签 → 目录（自动落盘）」 | P1 滑动窗口/双指针/二分/回溯/栈/堆 → P2 动态规划/贪心 → P3 链表/二叉树/图论/矩阵 → P4 哈希 → P5 子串/数组 |
| 抓题面命令（GraphQL） | `SKILL.md` →「第 1 节」 | `question(titleSlug)`，返回 HTML 题面，附录 A 脚本转纯文本 |
| 抓官方题解命令（两步走） | `SKILL.md` →「第 2 节」 | `questionSolutionArticles` 列文章 → `solutionArticle` 取正文；附录 B 切 C++ 代码 |
| GraphQL 踩坑表 | `SKILL.md` →「第 2 节」末尾表 | `orderBy` 只用 `DEFAULT`；`UserNode` 不可查；introspection 被禁 |
| 笔记模板 + 文风 | `AGENTS.md` →「笔记文件规范」+ `SKILL.md` →「第 5 节」 | 题面/思路/代码/复杂度/疑惑(Q&A)/复盘；正文一份主解，其余进复盘 |
| 代码规范（C++17、STL 详注） | `AGENTS.md` →「代码风格」 | STL 必须行内注释：签名/含义/踩坑/复杂度 |
| 硬性约束 | `AGENTS.md` →「Agent 行为规则」 | 不改已有笔记、不捏造题面、不引工具链、不 git init |

---

## 三、执行模式：主 agent + subagent（每次 3 个并行）

本次批量执行**不由主 agent 逐题抓取**，而是：

- **主 agent（我）**：维护 [`进度.md`](./进度.md)、按批次分配任务、**审核** subagent 产出的文档（合格则保留、不合格则微调）。
- **subagent**：每次**同时开 3 个**，每个负责 1 道题。主 agent 在派发 prompt 里告诉它：题号/slug/落盘目录，并要求它**先读 `plan.md` 的「快速上手 5 步」和 `SKILL.md`**，再按流程抓取+写笔记。
- **数据来源单一**：所有题目清单来自 [`_hot100_list.json`](./_hot100_list.json)（已抓取的 100 题元数据），subagent 只需按指派的 slug 抓题面与官方题解，**不要自行编造题面**。

**审核标准（主 agent 判定合格与否）**：

1. 题面、数据范围、标签与力扣原文一致（不捏造）。
2. 主解是**力扣官方题解**的推荐解法，代码完整可编译、能直接贴进力扣。
3. STL 用法有**详细行内注释**（签名/含义/踩坑/复杂度）。
4. 落盘目录与 [`进度.md`](./进度.md) 指定一致；文件名 `<题号>-<题名简写>.md`。
5. 正文一份主解，其余解法在「复盘」；含「疑惑/卡点」Q&A 和「复盘」章节。

---

## 四、批次划分（87 题，每批 3 题，共 29 批）

按 [`进度.md`](./进度.md)「待办」表顺序，每 3 题一批并行执行。下表是批次预览（实时状态以 [`进度.md`](./进度.md) 为准）。

| 批次 | 题目（题号 题名 → 目录） |
|---|---|
| 批 1 | `239` 滑动窗口最大值 → `滑动窗口/`；`76` 最小覆盖子串 → `滑动窗口/`；`53` 最大子数组和 → `动态规划/` |
| 批 2 | `56` 合并区间 → `普通数组/`；`189` 轮转数组 → `双指针/`；`238` 除了自身以外数组的乘积 → `普通数组/` |
| 批 3 | `41` 缺失的第一个正数 → `哈希/`；`73` 矩阵置零 → `矩阵/`；`240` 搜索二维矩阵 II → `二分查找/` |
| 批 4 | `160` 相交链表 → `双指针/`；`206` 反转链表 → `链表/`；`234` 回文链表 → `双指针/` |
| 批 5 | `141` 环形链表 → `双指针/`；`142` 环形链表 II → `双指针/`；`21` 合并两个有序链表 → `链表/` |
| 批 6 | `2` 两数相加 → `链表/`；`19` 删除链表的倒数第 N 个结点 → `双指针/`；`24` 两两交换链表中的节点 → `链表/` |
| 批 7 | `25` K 个一组翻转链表 → `链表/`；`138` 随机链表的复制 → `链表/`；`148` 排序链表 → `双指针/` |
| 批 8 | `23` 合并 K 个升序链表 → `堆/`；`146` LRU 缓存 → `链表/`；`94` 二叉树的中序遍历 → `栈/` |
| 批 9 | `104` 二叉树的最大深度 → `二叉树/`；`226` 翻转二叉树 → `二叉树/`；`101` 对称二叉树 → `二叉树/` |
| 批 10 | `543` 二叉树的直径 → `二叉树/`；`102` 二叉树的层序遍历 → `二叉树/`；`108` 将有序数组转换为二叉搜索树 → `二叉树/` |
| 批 11 | `98` 验证二叉搜索树 → `二叉树/`；`230` 二叉搜索树中第 K 小的元素 → `二叉树/`；`199` 二叉树的右视图 → `二叉树/` |
| 批 12 | `114` 二叉树展开为链表 → `栈/`；`437` 路径总和 III → `二叉树/`；`236` 二叉树的最近公共祖先 → `二叉树/` |
| 批 13 | `124` 二叉树中的最大路径和 → `动态规划/`；`200` 岛屿数量 → `图论/`；`994` 腐烂的橘子 → `图论/` |
| 批 14 | `207` 课程表 → `图论/`；`208` 实现 Trie (前缀树) → `图论/`；`46` 全排列 → `回溯/` |
| 批 15 | `78` 子集 → `回溯/`；`17` 电话号码的字母组合 → `回溯/`；`39` 组合总和 → `回溯/` |
| 批 16 | `22` 括号生成 → `回溯/`；`79` 单词搜索 → `回溯/`；`131` 分割回文串 → `回溯/` |
| 批 17 | `51` N 皇后 → `回溯/`；`35` 搜索插入位置 → `二分查找/`；`74` 搜索二维矩阵 → `二分查找/` |
| 批 18 | `34` 在排序数组中查找元素的第一个和最后一个位置 → `二分查找/`；`33` 搜索旋转排序数组 → `二分查找/`；`153` 寻找旋转排序数组中的最小值 → `二分查找/` |
| 批 19 | `4` 寻找两个正序数组的中位数 → `二分查找/`；`20` 有效的括号 → `栈/`；`155` 最小栈 → `栈/` |
| 批 20 | `394` 字符串解码 → `栈/`；`739` 每日温度 → `栈/`；`84` 柱状图中最大的矩形 → `栈/` |
| 批 21 | `215` 数组中的第K个最大元素 → `堆/`；`347` 前 K 个高频元素 → `堆/`；`295` 数据流的中位数 → `双指针/` |
| 批 22 | `121` 买卖股票的最佳时机 → `动态规划/`；`55` 跳跃游戏 → `动态规划/`；`45` 跳跃游戏 II → `动态规划/` |
| 批 23 | `763` 划分字母区间 → `双指针/`；`70` 爬楼梯 → `动态规划/`；`118` 杨辉三角 → `动态规划/` |
| 批 24 | `198` 打家劫舍 → `动态规划/`；`279` 完全平方数 → `动态规划/`；`322` 零钱兑换 → `动态规划/` |
| 批 25 | `139` 单词拆分 → `动态规划/`；`300` 最长递增子序列 → `二分查找/`；`152` 乘积最大子数组 → `动态规划/` |
| 批 26 | `416` 分割等和子集 → `动态规划/`；`32` 最长有效括号 → `栈/`；`62` 不同路径 → `多维动态规划/` |
| 批 27 | `64` 最小路径和 → `多维动态规划/`；`5` 最长回文子串 → `双指针/`；`1143` 最长公共子序列 → `多维动态规划/` |
| 批 28 | `72` 编辑距离 → `多维动态规划/`；`136` 只出现一次的数字 → `普通数组/`；`169` 多数元素 → `哈希/` |
| 批 29 | `75` 颜色分类 → `双指针/`；`31` 下一个排列 → `双指针/`；`287` 寻找重复数 → `双指针/` |

> 难度分布参考：HARD 题会明显更慢（如 239 滑动窗口最大值、76 最小覆盖子串、4 寻找两个正序数组的中位数等），派发时可适当把 HARD 题分散到不同批次，避免单批 3 题都卡在难题上。

---

## 五、已知边界情况（重要，别踩）

1. **`128 最长连续序列` 现在在 `哈希/`**：它的标签含「并查集」，按 AGENTS.md 标签优先级并查集属图论，规则上似乎该落 `图论/`。但力扣官方把它分在「哈希」组（核心解就是哈希集），且**它是已有笔记**。**结论：保留现状不动**（遵守「不改已有笔记」），也不要新建 `图论/128-...` 的重复文件。后续遇到「标签含并查集但主解是哈希」的新题，主解是哈希就落 `哈希/`。

2. **力扣分组名 ≠ 仓库目录名的情况**：力扣「子串」组里的 `239 滑动窗口最大值`（标签含滑动窗口）按优先级落 `滑动窗口/`，不是 `子串/`。以 [`进度.md`](./进度.md) 的「落盘目录」列为准，不要按力扣分组名落盘。

3. **官方题解 `content` 字段可能截断**：长 C++ 代码（尤其 HARD 题）在 `content` 里可能被截断粘连。subagent 切出代码后**必须人眼核对完整性**，不完整时去题目页题解 Tab 取完整版，不要把残缺代码写进笔记（SKILL.md 附录 B 已警告）。

4. **`贪心/` 目录已改名**：原 `贪心算法/` 已按 AGENTS.md 改为 `贪心/`（已操作），后续贪心题落 `贪心/`。

---

## 六、临时文件约定

- 抓取过程的中间文件（`_q.json` / `_sol.json` / `_official.json` 等）是临时产物，**每题做完即清理**。
- 长期保留：[`_hot100_list.json`](./_hot100_list.json)（100 题元数据）、[`进度.md`](./进度.md)（进度追踪）、本 `plan.md`。
